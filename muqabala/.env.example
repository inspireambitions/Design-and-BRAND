# Optional. Without this key the app runs in demo mode:
# answers are scored by the built-in heuristic scorer instead of Claude.
# Add it in Vercel → Project → Settings → Environment Variables to switch on real AI scoring.
ANTHROPIC_API_KEY=
